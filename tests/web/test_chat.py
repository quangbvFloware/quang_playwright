from playwright.sync_api import expect

from framework.utils.logging_util import logger
from framework.web.pages.login_page import WebLoginPage


def test_chat(api_clients, web_clients):
    owner_api = api_clients.owner
    # owner_web = web_clients.owner
    breakpoint()
    # owner.channel_page.open(owner.base_url)
    # owner.channel_page.create_channel("Test Channel", "Test Description")
    # member.channel_page.open(member.base_url)
    # member.channel_page.join_channel("Test Channel")
    # member.channel_page.send_message("Hello, world!")
    # owner.channel_page.open(owner.base_url)
    # owner.channel_page.send_message("Hello, world!")
    # owner.channel_page.open(owner.base_url)
